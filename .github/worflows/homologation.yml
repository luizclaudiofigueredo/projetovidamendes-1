name: Deploy to Atlantic.net

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Install SSH key
      uses: webfactory/ssh-agent@v0.5.3
      with:
        ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

    - name: Deploy to Atlantic.net
      run: |
        ssh -o StrictHostKeyChecking=no luiz@104.245.35.79 'bash -s' < deploy_script.sh
